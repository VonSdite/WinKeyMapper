<Window x:Class="KeyMapper.AppDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="提示" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource Card}" Padding="18">
            <StackPanel>
                <TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold"/>
                <TextBlock x:Name="MessageText" Margin="0,10,0,0"
                           TextWrapping="Wrap" Foreground="{StaticResource FgBrush}"/>
                <Border x:Name="DetailBox" Margin="0,14,0,0" Padding="12"
                        Background="#F8FAFC" BorderBrush="{StaticResource CardBorderBrush}"
                        BorderThickness="1" CornerRadius="6">
                    <TextBlock x:Name="DetailText" TextWrapping="Wrap"
                               Foreground="{StaticResource MutedBrush}"/>
                </Border>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="CancelButton" Content="取消" Style="{StaticResource SecondaryButton}"
                    IsCancel="True" Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button x:Name="PrimaryButton" Content="确定" Style="{StaticResource AccentButton}"
                    IsDefault="True" Click="Primary_Click"/>
        </StackPanel>
    </Grid>
</Window>
