<Window x:Class="KeyMapper.MappingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="映射" Height="620" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{StaticResource BgBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 源键 -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Width="28" Height="28" CornerRadius="14"
                    Background="{StaticResource SelectedBrush}" Margin="0,0,12,0"
                    VerticalAlignment="Top">
                <TextBlock Text="1" Foreground="{StaticResource AccentBrush}"
                           FontWeight="SemiBold" HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
            </Border>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,3,16,0">
                <TextBlock Text="源键" FontWeight="SemiBold"/>
                <TextBlock x:Name="SourceHint" Text="按下要改写的键"
                           Foreground="{StaticResource MutedBrush}" FontSize="11"
                           Margin="0,4,0,0"/>
            </StackPanel>
            <Border Grid.Column="2" x:Name="SourceKeyCap"
                    Background="#FFFFFF" BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="22,12"
                    Cursor="Hand" MouseLeftButtonDown="CaptureArea_MouseDown"
                    VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="6" Opacity="0.12"
                                      Direction="270" Color="Black"/>
                </Border.Effect>
                <TextBlock x:Name="SourceDisplay" Text="—" FontSize="20"
                           FontWeight="SemiBold" MinWidth="64" TextAlignment="Center"/>
            </Border>
        </Grid>

        <!-- 目标键 -->
        <Grid Grid.Row="1" Margin="0,0,0,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Width="28" Height="28" CornerRadius="14"
                        Background="{StaticResource SelectedBrush}" Margin="0,0,12,0"
                        VerticalAlignment="Top">
                    <TextBlock Text="2" Foreground="{StaticResource AccentBrush}"
                               FontWeight="SemiBold" HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,3,0,0">
                    <TextBlock Text="目标键" FontWeight="SemiBold"/>
                    <TextBlock Text="搜索或直接从列表选择" Foreground="{StaticResource MutedBrush}"
                               FontSize="11" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>

            <TextBox Grid.Row="1" x:Name="FilterBox" Style="{StaticResource SearchBox}"
                     Tag="搜索按键名称…" TextChanged="Filter_TextChanged" Margin="0,0,0,10"/>

            <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
                <ListBox x:Name="TargetList"
                         SelectionChanged="Target_SelectionChanged"
                         MouseDoubleClick="TargetList_MouseDoubleClick">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Display}" Padding="14,8"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    <ListBox.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.ContainerStyle>
                                <Style TargetType="GroupItem">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                        <ControlTemplate TargetType="GroupItem">
                                            <StackPanel>
                                                <Border Background="#F8FAFC"
                                                        BorderBrush="{StaticResource CardBorderBrush}"
                                                        BorderThickness="0,1,0,1"
                                                        Padding="14,8"
                                                        Margin="0,8,0,0">
                                                    <TextBlock Text="{Binding Name}" FontSize="12"
                                                               FontWeight="SemiBold"
                                                               Foreground="{StaticResource FgBrush}"/>
                                                </Border>
                                                <ItemsPresenter/>
                                            </StackPanel>
                                        </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </GroupStyle.ContainerStyle>
                        </GroupStyle>
                    </ListBox.GroupStyle>
                </ListBox>
            </Border>
        </Grid>

        <!-- 按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Style="{StaticResource SecondaryButton}" IsCancel="True"
                    Margin="0,0,8,0" Click="Cancel_Click"/>
            <Button x:Name="OkButton" Content="保存映射" Style="{StaticResource AccentButton}"
                    IsDefault="True" IsEnabled="False" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
